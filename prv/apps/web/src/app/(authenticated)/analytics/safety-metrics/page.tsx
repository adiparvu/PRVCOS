import { SafetyMetricsClient } from "./SafetyMetricsClient"

export const metadata = { title: "Safety Metrics · PRV" }

export default function SafetyMetricsPage() {
  return <SafetyMetricsClient />
}
