import { ToolInventoryClient } from "./ToolInventoryClient"

export const metadata = { title: "Tool Inventory · PRV" }

export default function ToolInventoryPage() {
  return <ToolInventoryClient />
}
