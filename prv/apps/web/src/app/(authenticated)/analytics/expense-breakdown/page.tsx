import { ExpenseBreakdownClient } from "./ExpenseBreakdownClient"

export const metadata = { title: "Expense Breakdown · PRV" }

export default function ExpenseBreakdownPage() {
  return <ExpenseBreakdownClient />
}
