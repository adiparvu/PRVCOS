import { FleetReadinessClient } from "./FleetReadinessClient"

export const metadata = { title: "Fleet Readiness · PRV" }

export default function FleetReadinessPage() {
  return <FleetReadinessClient />
}
