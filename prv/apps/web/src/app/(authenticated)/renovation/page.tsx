import { RenovationWorkspace } from "./RenovationWorkspace"

export const metadata = { title: "Renovation Projects · PRV" }

export default function RenovationPage() {
  return <RenovationWorkspace />
}
